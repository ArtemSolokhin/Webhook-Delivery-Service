# Webhook-Delivery-Service
A lightweight service for reliable webhook delivery. It allows applications to register webhook endpoints and send events to them asynchronously, with automatic retries when delivery fails.  The project focuses on reliable event delivery, idempotency, retry handling, and tracking the status of each delivery attempt.
